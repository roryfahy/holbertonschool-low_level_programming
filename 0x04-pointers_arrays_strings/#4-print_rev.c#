#include "holberton.h"
/**
 * print_rev - prints a string in reverse followed by a newline
 * @s: pointer to char
 *
 * Return: void
 */
void print_rev(char *s)
{
	while (*s)
	{
		_putchar(*str);
		str++;
	}
	_putchar('\n');
}
