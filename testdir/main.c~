#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void)
{
	char *line = NULL;
	size_t len = 0;
	ssize_t read;

	write(STDOUT_FILENO, "$ ", 2);
	read = getline(&line, &len, stdin);
		if (read == -1)
			exit(EXIT_FAILURE);

	free(line);
	exit(EXIT_SUCCESS);
}
