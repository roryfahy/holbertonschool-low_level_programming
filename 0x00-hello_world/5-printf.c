#include <stdio.h>
/**
 * main - this functions uses the printf command
 *
 * Return: 0 indicates succesful run
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
