#include <stdio.h>
#include <stdlib.h>
#include "holberton.h"
/**
 * main - adds positive numbers passed in on the command line
 * @argc: int
 * @argv: pointer to array of strings
 *
 * Return: zero if success
 */
int main(int argc, char *argv[])
{
	if (atoi(argv[i] 
		return (1);
	}
	printf("%d\n", atoi(argv[1]) * atoi(argv[2]));
	return (0);
}
