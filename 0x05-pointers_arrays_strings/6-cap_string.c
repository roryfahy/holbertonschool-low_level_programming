#include "holberton.h"
/**
 * *cap_string - capitalized the words of a str
 * @zard: points to a char str
 *
 * Return: ponter to s
 */
char *cap_string(char *zard)
{
	char *pika = zard;

	for (; *pika; pika++)
	{
		if (*pika == '

			return (zard);
}
